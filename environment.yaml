# Minimal environment for container generation; tests use tests/environment.yaml.
# Workflow users may install Snakemake by any supported method.
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  - python=3.12
  - snakemake-minimal=9.8.0
  - pytest=8.4.1
